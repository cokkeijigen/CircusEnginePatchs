project(UtilTest)

project_yy_thunks_for_win2k_release_only()

file(GLOB_RECURSE SOURCES "${CMAKE_CURRENT_LIST_DIR}/src/*.cpp")
add_executable(UtilTest ${SOURCES})

file(GLOB HEADER_DIR ALL_FILES_AND_DIRS 
    "${CMAKE_CURRENT_LIST_DIR}"
    "${CMAKE_CURRENT_LIST_DIR}/*"
    "${CMAKE_CURRENT_LIST_DIR}/*/*"
    "${CMAKE_CURRENT_LIST_DIR}/*/*/*"
)
target_include_directories(UtilTest PUBLIC ${HEADER_DIR})
target_link_libraries(UtilTest  ${Utillibs})
